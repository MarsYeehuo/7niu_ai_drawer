# AI 语音绘图工具 — 设计文档

## 一、概述

本文档记录 AI 语音绘图工具的设计思路、计划支持的指令能力、实际实现情况，以及未完成部分的说明。

### 系统架构

```
┌─────────────────────┐     WebSocket      ┌──────────────────┐     HTTPS     ┌──────────────┐
│  浏览器 (Frontend)   │ ◄──────────────► │  Python FastAPI  │ ◄──────────► │  LLM API     │
│                     │      JSON 指令     │  (Backend)       │  指令解析    │ (Anthropic   │
│  Canvas 绘图        │                    │  LLM Client      │              │  格式)        │
│  Web Speech API     │                    │  (可配置模型)     │              │               │
│  Speech Synthesis   │                    │                  │              │               │
└─────────────────────┘                    └──────────────────┘              └──────────────┘
```

### 核心流程

1. 用户按「开始语音输入」→ 浏览器启动 Web Speech API 连续识别
2. 用户说话 → 语音转为文本 → 用户停顿 1.5 秒 → 自动提交
3. 文本 + 模型名称 + 画布上下文经 WebSocket 发送至后端
4. 后端使用 Anthropic SDK 格式调用 LLM（默认 `deepseek-v4-flash`，前端可切换）
5. LLM 返回结构化 JSON 绘图指令数组
6. 后端透传至前端 → Canvas 逐条执行 → TTS 播报反馈
7. 识别自动重启，等待下一条指令

### 模型可配置性

- `LLM_BASE_URL` 环境变量指定 API 端点，支持切换到任何兼容 Anthropic 格式的 LLM 服务
- `DEFAULT_LLM_MODEL` 环境变量指定默认模型
- 前端 UI 可实时切换模型名称，每次请求携带当前模型名

---

## 二、计划支持的指令能力

### L1 — 基础能力（核心）

| 指令 | 说明 |
|------|------|
| 画基本形状 | 圆形、矩形/正方形、三角形、直线、椭圆、点 |
| 颜色选择 | 通过颜色名称指定（红、蓝、绿、黄等 20+ 种） |
| 画布清空 | 清除所有绘图内容 |
| 撤销 | 回退上一次操作 |

### L2 — 控制能力

| 指令 | 说明 |
|------|------|
| 画布尺寸调整 | 语音指定新尺寸，如"把画布设为1024x768" |
| 尺寸指定 | 说"大/小"或具体像素值 |
| 位置指定 | 左上角、中间、右下角等九宫格位置 |
| 文本绘制 | 在指定位置绘制文字 |
| 背景设置 | 设置画布背景颜色 |

### L3 — 组合能力

| 指令 | 说明 |
|------|------|
| 相对定位 | "在红色圆的右边画一个蓝色正方形" |
| 复合物体 | "画一个房子" → 自动拆解为矩形+三角形 |
| 属性修改 | "把这个改成蓝色"、"把它放大一倍"（需上下文） |

### L4 — 创意能力

| 指令 | 说明 |
|------|------|
| 场景描述 | "画一个宁静的乡村傍晚" → 组合多个基本形状形成场景 |

---

## 三、实际实现情况

| 能力 | 计划 | 实现 | 说明 |
|------|------|------|------|
| 基本形状（圆/矩形/三角/线/椭圆/点） | ✅ | ✅ | 6 种形状全部支持 |
| 颜色选择（颜色名称 + hex） | ✅ | ✅ | 20+ 颜色名称 + 任意 hex |
| 画布清空 | ✅ | ✅ | |
| 撤销 | ✅ | ✅ | 支持多级撤销 |
| 画布尺寸调整 | ✅ | ✅ | |
| 尺寸指定（大/小/具体值） | ✅ | ✅ | 由 LLM 自动计算 |
| 位置指定（九宫格） | ✅ | ✅ | |
| 文本绘制 | ✅ | ✅ | |
| 背景设置 | ✅ | ✅ | |
| 相对定位 | ✅ | ✅ | 通过画布上下文 + LLM 空间推理 |
| 复合物体拆解 | ✅ | ✅ | 由 LLM 自动拆解 |
| 属性修改（改颜色/大小） | ✅ | ✅ | 通过画布上下文 + 指代消解 |
| 场景描述 | ✅ | ✅ | 由 LLM 创意生成 |
| 模型可配置（前端切换） | — | ✅ | 输入框自由切换模型名 |
| 语音识别连续模式 | — | ✅ | 自动重启，持续聆听 |
| 静音自动提交 | — | ✅ | 1.5 秒静音后自动发送 |
| TTS 防反馈回路 | — | ✅ | 播报时暂停语音识别 |
| 断线重连 | — | ✅ | WebSocket 断开自动重连 |
| WebSocket 实时通信 | — | ✅ | |
| 画布状态上下文传递 | — | ✅ | 每次请求携带当前所有对象 |
| 键盘快捷键（空格切换） | — | ✅ | |

### Prompt 设计要点

系统 prompt 包含：
- **Canvas 坐标系统定义**：原点左上角，x→右，y→下，单位像素
- **支持的动作列表**：6 种绘图动作 + 4 种画布控制
- **支持的形状与必填字段**
- **颜色名称表** + 允许 hex 码
- **输出 JSON 格式约束**
- **复合物体拆解规则**：复杂物体拆为多个基本形状

---

## 四、设计决策与取舍

### 为什么要全部走 LLM？

- **规则引擎的局限**：自然语言表达方式几乎无限，规则引擎无法覆盖所有变体
- **上下文理解**：LLM 能理解"它"、"那个"等指代
- **复合拆解**："房子"→矩形+三角形 这种常识性拆解对 LLM 非常自然
- **创意生成**：场景描述需要真正的语义理解

### 为什么用 Anthropic SDK 格式实现多模型兼容？

- Anthropic SDK 的消息格式（system + messages）是业界广泛兼容的标准格式
- 通过配置 `base_url` 即可切换到 DeepSeek、OpenAI 等支持此格式的服务
- 未来切换或升级模型无需修改代码

### 为什么不维护后端会话状态？

- **无状态设计更简单**：前端维护画布状态，每次请求携带完整上下文
- **容错性好**：后端重启不影响前端已有绘图
- **LLM 每次拿到完整上下文**：指代消解更准确

### 为什么不使用流式语音识别中间结果？

- Web Speech API 的中间结果变化频繁，直接发给 LLM 会造成大量无意义调用
- 方案：等待用户停顿 1.5 秒后提交 final 结果，平衡实时性和准确性

---

## 五、未完成部分及原因

| 未完成功能 | 原因 |
|-----------|------|
| **画布导出/保存为图片** | 增强功能而非核心语音绘图能力，不影响主要交互闭环 |
| **多语言支持（英文指令）** | 当前聚焦中文场景。LLM 本身支持多语言，识别层限定了 zh-CN |
| **触摸屏/移动端适配** | 语音绘图主要面向桌面场景 |
| **自定义颜色拾取（语音调色板）** | 当前通过颜色名称 + hex 码覆盖常用需求 |
| **图层/分组管理** | 当前优先保证基础流程稳定 |
| **指令确认/纠错对话框** | 设计目标是"无需鼠标或键盘"，当前策略：LLM 做最佳猜测 + 语音播报结果 |

## 六、潜在改进方向

1. **指令确认**：TTS 播报后语音确认或重试
2. **记忆上次指令**：支持"和刚才一样但改成蓝色"
3. **画布缩放/平移**：通过语音控制视口
4. **多步撤销/重做栈可视化**
5. **画布快照**：语音"保存当前画面"
